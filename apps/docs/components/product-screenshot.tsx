/** Screenshots were removed from the repo; keep the MDX tag as a no-op. */
export function ProductScreenshot(_props: {
  name: string;
  alt: string;
  caption?: string;
  position?: "center" | "top";
}) {
  return null;
}
